---
interface Props {
  className?: string;
}

const { className } = Astro.props;
---

<h2
  class:list={[
    "flex items-center mb-6 text-3xl font-semibold gap-x-3 text-white",
    className,
  ]}
>
  <slot />
</h2>
